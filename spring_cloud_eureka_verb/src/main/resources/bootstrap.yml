# spring application name
spring:
  application:
    name: eureka-verb
  cloud:
      config:
        uri: http://localhost:9030
  profiles:
    active: verb

# spring application port
server:
  port: 9011
