# spring application name
spring:
  application:
    name: eureka-noun
  cloud:
      config:
        uri: http://localhost:9030
  profiles:
    active: noun

# spring application port
server:
  port: 9014
