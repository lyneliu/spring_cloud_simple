# spring application name
spring:
  application:
    name: eureka-adjective
  profiles:
    active: adjective

# spring application port
server:
  port: 9013


# 调用配置信息
spring:
  application:
    name: config-server
  cloud:
    config:
      uri: http://localhost:9001