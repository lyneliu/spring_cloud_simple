# spring application name
spring:
  application:
    name: eureka-adjective
  cloud:
      config:
        uri: http://localhost:9030
  profiles:
    active: adjective

# spring application port
server:
  port: 9013
