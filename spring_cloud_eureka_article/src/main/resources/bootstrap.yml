# spring application name
spring:
  application:
    name: eureka-article
  cloud:
      config:
        uri: http://localhost:9030
  profiles:
    active: article

# spring application port
server:
  port: 9012
