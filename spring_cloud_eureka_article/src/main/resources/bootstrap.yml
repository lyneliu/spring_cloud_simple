# spring application name
spring:
  application:
    name: eureka-article

# spring application port
server:
  port: 9012


# 调用配置信息
spring:
  application:
    name: config-server
  cloud:
    config:
      uri: http://localhost:9001