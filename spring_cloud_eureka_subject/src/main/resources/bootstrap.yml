# spring application name
spring:
  application:
    name: eureka-subject
  cloud:
      config:
        uri: http://localhost:9030
  profiles:
    active: subject

# spring application port
server:
  port: 9010
