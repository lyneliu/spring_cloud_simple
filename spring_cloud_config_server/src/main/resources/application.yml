# spring security
security:
  basic:
    enabled: true

# spring cloud config
spring:
  cloud:
    config:
      server:
        git:
          uri: https://git.oschina.net/.lyne/spring_cloud_simple/
          search-paths: spring-cloud-config
        svn:
          uri:
          search-paths:
      enabled: false