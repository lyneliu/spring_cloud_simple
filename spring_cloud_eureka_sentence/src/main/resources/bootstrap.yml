# spring cloud应用名称
spring:
  application:
    name: eurake-sentence

# spring application port
server:
  port: 9020


# 调用配置信息
spring:
  application:
    name: config-server
  cloud:
    config:
      uri: http://localhost:9030